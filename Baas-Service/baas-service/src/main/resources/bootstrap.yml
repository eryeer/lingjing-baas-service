spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${DISCOVERY:172.168.3.12:8848} # register center
      config:
        server-addr: ${CONFIG:172.168.3.12:8848} # config center
        file-extension: yml